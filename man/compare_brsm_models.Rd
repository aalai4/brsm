% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_brsm_models.R
\name{compare_brsm_models}
\alias{compare_brsm_models}
\title{Compare Bayesian Models for brsm}
\usage{
compare_brsm_models(models, criterion = c("loo", "waic"), ...)
}
\arguments{
\item{models}{A named list of \code{brsm_fit} and/or \code{brmsfit}
objects.}

\item{criterion}{Comparison criterion: \code{"loo"} or \code{"waic"}.}

\item{...}{Additional arguments passed to \code{brms::loo()} or
\code{brms::waic()}.}
}
\value{
A list with components:
  \code{criterion}, \code{estimates} (named list of criterion objects), and
  \code{comparison} (data frame from \code{loo_compare}).
}
\description{
Computes model comparison criteria across multiple fitted Bayesian models.
}
\examples{
\dontrun{
fit1 <- fit_brsm(dat, response = "y", factor_names = c("x1", "x2"))
fit2 <- fit_brsm(dat,
  response = "y", factor_names = c("x1", "x2"),
  prior = brms::prior(normal(0, 1), class = "b")
)
cmp <- compare_brsm_models(
  list(default = fit1, stronger_prior = fit2), "loo"
)
cmp$comparison
}
}
