% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical_analysis.R
\name{canonical_analysis}
\alias{canonical_analysis}
\alias{canonical_analysis.brsm_fit}
\alias{canonical_analysis.brmsfit}
\alias{canonical_analysis.default}
\title{Canonical Analysis of Bayesian Response Surface}
\usage{
canonical_analysis(
  object,
  factor_names = NULL,
  include_scores = TRUE,
  kappa_thresh = 1e+10,
  probs = c(0.025, 0.5, 0.975),
  summary = TRUE
)

\method{canonical_analysis}{brsm_fit}(
  object,
  factor_names = NULL,
  include_scores = TRUE,
  kappa_thresh = 1e+10,
  probs = c(0.025, 0.5, 0.975),
  summary = TRUE
)

\method{canonical_analysis}{brmsfit}(
  object,
  factor_names = NULL,
  include_scores = TRUE,
  kappa_thresh = 1e+10,
  probs = c(0.025, 0.5, 0.975),
  summary = TRUE
)

\method{canonical_analysis}{default}(
  object,
  factor_names = NULL,
  include_scores = TRUE,
  kappa_thresh = 1e+10,
  probs = c(0.025, 0.5, 0.975),
  summary = TRUE
)
}
\arguments{
\item{object}{A \code{brsm_fit} object, \code{brmsfit} object, or data frame
of posterior draws with Bayesian coefficient columns.}

\item{factor_names}{Character vector of factor names (required when
\code{object} is a data frame).}

\item{include_scores}{Logical; if \code{TRUE} (default), compute canonical
factor scores at the stationary point for each draw.}

\item{kappa_thresh}{Condition number threshold for Hessian singularity.
Draws whose Hessian condition number exceeds this are excluded from score
computation.}

\item{probs}{Probability levels for posterior summary intervals.}

\item{summary}{Logical; if \code{TRUE} (default), return a compact summary
data frame with posterior means and credible intervals per canonical
component. If \code{FALSE}, return per-draw eigenvalues, eigenvectors, and
(optionally) canonical scores as a list.}
}
\value{
When \code{summary = TRUE}: a list with components
  \code{eigenvalues} (data frame of posterior summaries for each principal
  curvature), \code{eigenvectors} (data frame of posterior summaries for
  each axis loading), and optionally \code{scores} (data frame of posterior
  summaries for canonical factor scores at \eqn{x^*}).

  When \code{summary = FALSE}: a list with components \code{eigenvalues}
  (draws × p matrix), \code{eigenvectors} (draws × p × p array), and
  optionally \code{scores} (draws × p matrix of canonical factor scores).
}
\description{
Performs canonical analysis by computing the eigendecomposition of the
Hessian matrix (B matrix) of the quadratic response surface across all
posterior draws. Returns posterior distributions of principal curvatures
(eigenvalues) and canonical axes (eigenvectors), along with canonical factor
scores at the stationary point.
}
\details{
The Hessian \eqn{\mathbf{B}} of the quadratic surface
\eqn{y = \beta_0 + \mathbf{b}^\top\mathbf{x} + \mathbf{x}^\top\mathbf{B}\mathbf{x}}
has eigendecomposition \eqn{\mathbf{B} = \mathbf{M}\boldsymbol{\Lambda}\mathbf{M}^\top},
where the columns of \eqn{\mathbf{M}} are the canonical axes and the
diagonal of \eqn{\boldsymbol{\Lambda}} are the principal curvatures.
}
\examples{
\dontrun{
fit <- fit_brsm(dat, response = "y", factor_names = c("x1", "x2"))
ca <- canonical_analysis(fit)
ca$eigenvalues   # posterior summary of principal curvatures
ca$eigenvectors  # posterior summary of axis loadings
ca$scores        # canonical factor scores at stationary point
}

}
