% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_predict_brsm.R
\name{posterior_predict_brsm}
\alias{posterior_predict_brsm}
\alias{posterior_predict_brsm.brsm_fit}
\alias{posterior_predict_brsm.brmsfit}
\alias{posterior_predict_brsm.default}
\title{Posterior Predictive Draws at New Points}
\usage{
posterior_predict_brsm(
  object,
  factor_names,
  newdata,
  include_residual = TRUE,
  sigma = NULL,
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  draw_subset = NULL,
  max_draws = NULL,
  return_matrix = FALSE,
  output_chunk_size = NULL,
  seed = NULL
)

\method{posterior_predict_brsm}{brsm_fit}(
  object,
  factor_names = NULL,
  newdata,
  include_residual = TRUE,
  sigma = NULL,
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  draw_subset = NULL,
  max_draws = NULL,
  return_matrix = FALSE,
  output_chunk_size = NULL,
  seed = NULL
)

\method{posterior_predict_brsm}{brmsfit}(
  object,
  factor_names,
  newdata,
  include_residual = TRUE,
  sigma = NULL,
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  draw_subset = NULL,
  max_draws = NULL,
  return_matrix = FALSE,
  output_chunk_size = NULL,
  seed = NULL
)

\method{posterior_predict_brsm}{default}(
  object,
  factor_names,
  newdata,
  include_residual = TRUE,
  sigma = NULL,
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  draw_subset = NULL,
  max_draws = NULL,
  return_matrix = FALSE,
  output_chunk_size = NULL,
  seed = NULL,
  .sigma_source_draws = NULL
)
}
\arguments{
\item{object}{A \code{brsm_fit} object, \code{brmsfit} object, or data
frame of posterior draws.}

\item{factor_names}{Character vector of factor names. For
\code{brsm_fit}, defaults to metadata when omitted.}

\item{newdata}{Data frame of predictor values at which to predict.}

\item{include_residual}{Logical; if \code{TRUE} (default), include
residual noise using posterior \code{sigma} draws.}

\item{sigma}{Optional residual SD specification. If \code{NULL}, the
function attempts to infer \code{sigma} from posterior draw columns.
Can be a scalar or one value per posterior draw.}

\item{summary}{Logical; if \code{TRUE}, return posterior summaries per row.}

\item{probs}{Probabilities used for quantile summaries when
\code{summary = TRUE}.}

\item{draw_subset}{Optional logical or numeric draw subset.}

\item{max_draws}{Optional cap on number of draws.}

\item{return_matrix}{Logical; if \code{TRUE} and \code{summary = FALSE},
return a draw-by-point matrix instead of long-format data.}

\item{output_chunk_size}{Optional chunk size for long-format assembly when
\code{summary = FALSE} and \code{return_matrix = FALSE}.}

\item{seed}{Optional random seed for predictive noise draws.}
}
\value{
A prediction object as matrix, long-format data frame, or summary
  data frame depending on options.
}
\description{
Generates posterior predictive values at arbitrary new predictor points.
Unlike [predict_surface()], this function can include residual uncertainty
(sigma), so outputs are draws from the posterior predictive distribution
rather than posterior means alone.
}
\details{
Supports S3 dispatch for \code{brsm_fit}, \code{brmsfit}, and data frames
of posterior draws.
}
