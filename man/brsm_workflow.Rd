% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brsm_workflow.R
\name{brsm_workflow}
\alias{brsm_workflow}
\title{Run a Complete brsm Analysis Workflow}
\usage{
brsm_workflow(
  object,
  factor_names,
  ranges = NULL,
  probs = c(0.025, 0.5, 0.975),
  grid_n = 40,
  radii = seq(0, 1, length.out = 5),
  start = NULL,
  step_size = 0.1,
  n_steps = 10,
  include_plots = TRUE,
  contour_type = c("mean", "uncertainty", "quantile"),
  seed = NULL,
  fit_mode = c("none", "fit_brsm"),
  response = NULL,
  fit_args = list(),
  steps = c("predictions", "stationary", "classification", "credible_region",
    "steepest_ascent", "ridge", "plots")
)
}
\arguments{
\item{object}{Either (1) a \code{brsm_fit} object from [fit_brsm()],
(2) a \code{brmsfit} object from \code{brms::brm()},
(3) a posterior-draw data frame with Bayesian coefficient columns, or
(4) raw analysis data when \code{fit_mode != "none"}.}

\item{factor_names}{Character vector of factor names.}

\item{ranges}{Named list with factor ranges.}

\item{probs}{Posterior probabilities for prediction summaries.}

\item{grid_n}{Number of points per factor dimension in the prediction grid.}

\item{radii}{Numeric vector of radii for ridge analysis.}

\item{start}{Optional named numeric vector of starting values for steepest
ascent. Defaults to zeros for each factor.}

\item{step_size}{Step size for steepest ascent.}

\item{n_steps}{Number of steps for steepest ascent.}

\item{include_plots}{Logical; if `TRUE`, generate contour/optimum/ridge plots
when exactly two factors are provided.}

\item{contour_type}{Surface type for contour plotting. Passed to
[plot_posterior_contours()].}

\item{seed}{Optional random seed used for stochastic plotting operations.}

\item{fit_mode}{One of `"none"`, `"fit_brsm"`, or
`"fit_brsm"`. Use `"none"` (default) to analyze an existing
Bayesian fit/draw object. Use a fit mode to fit from raw data in `object`
and then run the workflow.}

\item{response}{Required when `fit_mode != "none"`. Name of the response
column in raw input data.}

\item{fit_args}{Named list of additional arguments forwarded to
[fit_brsm()] when `fit_mode != "none"`.
This enables custom priors and sampling controls without re-writing the
pipeline manually.}

\item{steps}{Character vector selecting which workflow stages to run. Any of
`"predictions"`, `"stationary"`, `"classification"`,
`"credible_region"`, `"steepest_ascent"`, `"ridge"`, `"plots"`.
Default runs all stages.}
}
\value{
A named list with components:
  always `draws`, and conditionally `fit`, `ranges`, `grid`, `predictions`,
  `surface`, `stationary_points`, `classification`, `credible_region`,
  `steepest_ascent`, `ridge_analysis`, and `plots` depending on `steps`.
}
\description{
Executes a full response-surface workflow from model/draw conversion through
prediction, optimization diagnostics, and optional plots.
}
\details{
By default (\code{fit_mode = "none"}), input must be Bayesian:
\code{brsm_fit}, \code{brmsfit}, or posterior-draw data frame with required
coefficient columns (\code{b_Intercept}, \code{b_x1}, \code{b_I(x1^2)},
interactions, etc.).

If \code{fit_mode} is \code{"fit_brsm"},
then \code{object} is treated as raw input data and model fitting is done
internally before running selected workflow steps.
}
\examples{
set.seed(1)
dat <- data.frame(
  x1 = runif(50, -2, 2),
  x2 = runif(50, -2, 2)
)
dat$y <- 5 + 2 * dat$x1 + 4 * dat$x2 - dat$x1^2 - 2 * dat$x2^2 +
  0.5 * dat$x1 * dat$x2 + rnorm(50, sd = 0.5)
draws_raw <- data.frame(
  b_Intercept = rnorm(200, 5, 0.2),
  b_x1 = rnorm(200, 2, 0.1),
  b_x2 = rnorm(200, 4, 0.1),
  "b_I(x1^2)" = rnorm(200, -1, 0.05),
  "b_I(x2^2)" = rnorm(200, -2, 0.05),
  "b_x1:x2" = rnorm(200, 0.5, 0.05),
  check.names = FALSE
)

out <- brsm_workflow(
  object = draws_raw,
  factor_names = c("x1", "x2"),
  ranges = list(x1 = c(-2, 2), x2 = c(-2, 2))
)

names(out)
}
