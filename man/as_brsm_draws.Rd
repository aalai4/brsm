% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_brsm_draws.R
\name{as_brsm_draws}
\alias{as_brsm_draws}
\alias{as_brsm_draws.brsm_fit}
\alias{as_brsm_draws.brmsfit}
\alias{as_brsm_draws.data.frame}
\alias{as_brsm_draws.default}
\title{Convert Model Output to brsm Draws Format}
\usage{
as_brsm_draws(object, factor_names, ...)

\method{as_brsm_draws}{brsm_fit}(object, factor_names = NULL, ...)

\method{as_brsm_draws}{brmsfit}(object, factor_names = NULL, ...)

\method{as_brsm_draws}{data.frame}(
  object,
  factor_names,
  require_quadratic = TRUE,
  require_interactions = TRUE,
  ...
)

\method{as_brsm_draws}{default}(object, factor_names, ...)
}
\arguments{
\item{object}{A `brsm_fit` object, `brmsfit` object, or a data frame of
posterior draws/coefficients.}

\item{factor_names}{Character vector of factor names (e.g., c("x1", "x2")).}

\item{...}{Additional arguments (currently unused).}
}
\value{
A data frame with brsm-compatible column names:
  - b_Intercept: intercept
  - b_<factor>: linear term for each factor
  - b_I(<factor>^2): quadratic term for each factor
  - b_<factor>:<factor>: interaction terms
}
\description{
Converts posterior draws to the brsm-compatible format with
standardized column names (b_Intercept, b_x1, b_I(x1^2), etc.).
}
\examples{
# From posterior draws in a data frame
draws_raw <- data.frame(
  b_Intercept = rnorm(100, 5, 0.2),
  b_x1 = rnorm(100, 2, 0.1),
  b_x2 = rnorm(100, 4, 0.1),
  "b_I(x1^2)" = rnorm(100, -1, 0.05),
  "b_I(x2^2)" = rnorm(100, -2, 0.05),
  "b_x1:x2" = rnorm(100, 0.5, 0.05),
  check.names = FALSE
)
draws <- as_brsm_draws(draws_raw, factor_names = c("x1", "x2"))
head(draws)

}
