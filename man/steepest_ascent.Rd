% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steepest_ascent.R
\name{steepest_ascent}
\alias{steepest_ascent}
\alias{steepest_ascent.brsm_fit}
\alias{steepest_ascent.default}
\title{Compute Steepest Ascent Path on Response Surface}
\usage{
steepest_ascent(
  object,
  factor_names = NULL,
  start = NULL,
  step_size = 1,
  n_steps = 10,
  tol = 1e-08,
  return_mean_path = TRUE,
  drop_null_paths = TRUE
)

\method{steepest_ascent}{brsm_fit}(
  object,
  factor_names = NULL,
  start = NULL,
  step_size = 1,
  n_steps = 10,
  tol = 1e-08,
  return_mean_path = TRUE,
  drop_null_paths = TRUE
)

\method{steepest_ascent}{default}(
  object,
  factor_names = NULL,
  start = NULL,
  step_size = 1,
  n_steps = 10,
  tol = 1e-08,
  return_mean_path = TRUE,
  drop_null_paths = TRUE
)
}
\arguments{
\item{object}{A `brsm_fit` object, `brmsfit` object, or
data frame of posterior draws with Bayesian coefficient columns.}

\item{factor_names}{Character vector of factor names
(if object is data frame).}

\item{start}{Starting point for ascent path
(default: center of coded region).}

\item{step_size}{Step size along gradient direction.}

\item{n_steps}{Number of steps to compute.}

\item{tol}{Numerical tolerance for gradient computations.}

\item{return_mean_path}{If TRUE, return mean path;
if FALSE, return all draws.}

\item{drop_null_paths}{Drop paths that don't increase response.}
}
\value{
Data frame with steepest ascent path coordinates and response values.
}
\description{
Computes the path of steepest ascent (or descent) on the response surface
starting from a specified point. Useful for sequential design of experiments
to locate the vicinity of the optimum.
}
