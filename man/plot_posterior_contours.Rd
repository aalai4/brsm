% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_posterior_contours.R
\name{plot_posterior_contours}
\alias{plot_posterior_contours}
\title{Plot Posterior Contours}
\usage{
plot_posterior_contours(
  draws,
  factor_names,
  ranges,
  n = 60,
  type = c("mean", "uncertainty", "quantile"),
  probs = c(0.025, 0.975),
  quantile = 0.5,
  bins = 10,
  vary_factors = NULL,
  fixed = NULL,
  conditioning = c("center", "optimum_mean", "user"),
  slice = NULL,
  pairwise = FALSE,
  overlay_stationary = FALSE,
  stationary_draws = NULL,
  overlay_type = c("mean", "posterior"),
  overlay_alpha = 0.1,
  overlay_max_draws = 2000,
  seed = NULL
)
}
\arguments{
\item{draws}{Validated draw data frame.}

\item{factor_names}{Character vector of factor names.}

\item{ranges}{Named list of factor ranges.}

\item{n}{Grid resolution.}

\item{type}{Surface type ("mean", "uncertainty", "quantile").}

\item{probs}{Probability quantiles for uncertainty.}

\item{quantile}{Quantile value if type="quantile".}

\item{bins}{Number of contour bins.}

\item{vary_factors}{Optional 2-factor pair for conditional plotting.}

\item{fixed}{Optional named numeric vector for fixed factor values.}

\item{conditioning}{Strategy for fixed factors.}

\item{slice}{Optional named list for sliced factor values.}

\item{pairwise}{Logical; if TRUE, generate all factor-pair panels.}

\item{overlay_stationary}{Logical; if TRUE, overlay stationary points.}

\item{stationary_draws}{Optional precomputed stationary draws.}

\item{overlay_type}{Type of stationary overlay ("mean" or "posterior").}

\item{overlay_alpha}{Transparency for posterior draw overlay.}

\item{overlay_max_draws}{Cap on posterior draws for overlay.}

\item{seed}{Random seed.}
}
\value{
A ggplot2 object.
}
\description{
Plot Posterior Contours
}
\details{
Contour fills use the viridis "turbo" palette, which is perceptually uniform
and colorblind-safe. Stationary points are overlaid in red (single-pair, non-sliced
contours only). For 3+ factors, use `pairwise=TRUE` for all pairs or `slice`
for conditional slices.
}
\keyword{internal}
