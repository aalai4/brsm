% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_brsm_ppc.R
\name{check_brsm_ppc}
\alias{check_brsm_ppc}
\title{Posterior Predictive Check Summary for a brsm Fit}
\usage{
check_brsm_ppc(
  object,
  ndraws = 200,
  probs = c(0.025, 0.975),
  seed = NULL,
  include_plot = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{brsm_fit} object from [fit_brsm()] or a
\code{brmsfit} object.}

\item{ndraws}{Number of posterior predictive draws.}

\item{probs}{Length-2 numeric vector of predictive interval probabilities.}

\item{seed}{Optional random seed.}

\item{include_plot}{Logical; if \code{TRUE}, includes a \code{pp_check}
histogram overlay plot in the output.}

\item{...}{Additional arguments passed to \code{brms::posterior_predict()}.}
}
\value{
A list with components:
  \code{summary} (one-row data frame), \code{observed} (numeric vector),
  \code{predicted_mean} (posterior mean prediction per observation), and
  optionally \code{plot}.
}
\description{
Computes a compact posterior predictive summary using simulated responses
from the fitted model.
}
\examples{
\dontrun{
fit <- fit_brsm(dat, response = "y", factor_names = c("x1", "x2"))
ppc <- check_brsm_ppc(fit, ndraws = 200)
ppc$summary
}
}
