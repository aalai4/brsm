% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_surface.R
\name{predict_surface}
\alias{predict_surface}
\alias{predict_surface.brsm_fit}
\alias{predict_surface.brmsfit}
\alias{predict_surface.default}
\title{Predict Response Surface from Posterior Draws or brsm_fit Objects}
\usage{
predict_surface(
  draws,
  factor_names,
  newdata,
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  draw_subset = NULL,
  max_draws = NULL,
  return_matrix = FALSE,
  output_chunk_size = NULL
)

\method{predict_surface}{brsm_fit}(
  draws,
  factor_names = NULL,
  newdata,
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  draw_subset = NULL,
  max_draws = NULL,
  return_matrix = FALSE,
  output_chunk_size = NULL
)

\method{predict_surface}{brmsfit}(
  draws,
  factor_names,
  newdata,
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  draw_subset = NULL,
  max_draws = NULL,
  return_matrix = FALSE,
  output_chunk_size = NULL
)

\method{predict_surface}{default}(
  draws,
  factor_names,
  newdata,
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  draw_subset = NULL,
  max_draws = NULL,
  return_matrix = FALSE,
  output_chunk_size = NULL
)
}
\arguments{
\item{draws}{A `brsm_fit` object, `brmsfit` object, or data frame of draws.}

\item{factor_names}{Character vector of factor names.}

\item{newdata}{Data frame of predictor values.}

\item{summary}{Logical; if `TRUE`, return posterior summaries per row.}

\item{probs}{Probabilities used for quantile summaries when `summary=TRUE`.}

\item{draw_subset}{Optional logical or numeric draw subset.}

\item{max_draws}{Optional cap on number of draws.}

\item{return_matrix}{Logical; if `TRUE` and `summary=FALSE`, return
draw-by-point matrix instead of long-format data frame.}

\item{output_chunk_size}{Optional chunk size for long-format assembly.}
}
\value{
A prediction object as matrix, long-format data frame, or summary
  data frame depending on options.
}
\description{
Supports S3 dispatch for `brsm_fit`, `brmsfit`, and draw data frames.
For `brsm_fit` objects, `factor_names` are inferred from metadata when
omitted.
}
